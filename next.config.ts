import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  // If you are trying to fix the workspace root warning:
  serverExternalPackages: [], 
};

export default nextConfig;